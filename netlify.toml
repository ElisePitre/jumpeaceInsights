[build]
  base = "src/templates/static"
  command = "npm run build && rm -rf ../netlify-dist && mkdir -p ../netlify-dist/public && cp index.html ../netlify-dist/index.html && cp -r ../public/. ../netlify-dist/public"
  publish = "../netlify-dist"

[build.environment]
  NODE_VERSION = "18"
